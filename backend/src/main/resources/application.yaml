spring:
  application:
    name: store
  datasource:
    url: jdbc:postgresql://localhost:5432/Ao_Thun
    username: postgres
    password: vu230319
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8080
logging:
  level:
    org.springframework: INFO
    com.example.TeeHub: DEBUG